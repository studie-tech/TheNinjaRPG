node_modules/
.env

# Generated by `cap add`; regenerate rather than committing half a project. Once the
# native projects are generated and committed, delete these two lines.
ios/
android/

# CocoaPods and Gradle build output, which stay ignored even after the projects land.
ios/App/Pods/
ios/App/App/public/
android/app/build/
android/build/
android/.gradle/
android/app/src/main/assets/public/
android/app/google-services.json

# Signing material must never enter the repository.
*.p8
*.p12
*.mobileprovision
*.keystore
*.jks

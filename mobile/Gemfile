source "https://rubygems.org"

# Release automation. `xcodeproj` is also what scripts/configure-xcode.rb uses to apply
# capabilities and the widget target to the generated project.
gem "fastlane"
gem "xcodeproj"
